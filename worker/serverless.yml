service: sitechecker-slackbot-worker

provider:
  name: aws
  runtime: nodejs4.3
  stage: ${opt:stage}
  region: ${opt:region}

package:
 include:
   - lib/

functions:
  check:
    handler: lib/sitechecker.check
